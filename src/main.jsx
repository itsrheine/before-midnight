import React from "react";
import { createRoot } from "react-dom/client";
import LoopPhone from "./LoopPhone";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <LoopPhone />
  </React.StrictMode>
);
